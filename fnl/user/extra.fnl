;; user.extra
;;
;; 额外配置入口（会在 user/config.fnl 末尾加载）。
;; 推荐用途：把“自动生成/可选”的插件 spec 放到这里，避免污染主配置。

;; nvim.sh managed plugins (auto)
;; - 由 picker 自动写入，请尽量不要手动编辑本区块
;; - 每行一个： (gentlewind.use_package {:repo "owner/repo"})
;; end nvim.sh managed plugins

{}
